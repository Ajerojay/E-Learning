import ParentDashboard from "./pages/Parent/ParentDashboard";
import "./AppParentDashboard.css";

// SUPABASE DATABASE: loads child, overall progress, and recent activity views.
export default function AppParentDashboard() {
  return <ParentDashboard />;
}

