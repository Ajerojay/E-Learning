import ParentProgress from "../pages/Parent/ParentProgress";
import "./AppParentProgress.css";

// Keep the mobile parent progress screen on the dedicated mobile layout and styling.
export default function AppParentProgress() {
  return <ParentProgress />;
}

